"use client";
import { useState } from "react";
type Message = { id: string; name: string; email: string; subject?: string; content: string; read: boolean; createdAt: string };
export function MessageManager({ initialMessages }: { initialMessages: Message[] }) { const [messages, setMessages] = useState(initialMessages); async function markRead(id: string) { const response = await fetch(`/api/admin/messages/${id}/read`, { method: "PATCH" }); if (response.ok) setMessages((current) => current.map((item) => item.id === id ? { ...item, read: true } : item)); } async function remove(id: string) { if (!window.confirm("Supprimer ce message ?")) return; const response = await fetch(`/api/admin/messages/${id}`, { method: "DELETE" }); if (response.ok) setMessages((current) => current.filter((item) => item.id !== id)); } return <div className="message-list">{messages.length === 0 && <p className="page-lead">Aucun message reçu.</p>}{messages.map((message) => <article className={`message-item ${message.read ? "is-read" : ""}`} key={message.id}><div><div className="message-heading"><strong>{message.name}</strong><span>{new Date(message.createdAt).toLocaleDateString("fr-FR")}</span></div><p>{message.email}{message.subject ? ` · ${message.subject}` : ""}</p><div className="message-content">{message.content}</div></div><div className="message-actions">{!message.read && <button className="quiet-button" onClick={() => markRead(message.id)}>Marquer lu</button>}<button className="danger-button" onClick={() => remove(message.id)}>Supprimer</button></div></article>)}</div>; }
